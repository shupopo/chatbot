# 作業工程: LangChain + Supabase RAGチャットボット

## 概要
既存の FAISS ベースのRAGチャットボットを **Supabase (pgvector)** に移行し、  
PDFアップロード → 質問応答が可能なチャットボットを Streamlit Community Cloud にデプロイする。

---

## フェーズ1: Supabase セットアップ

### 1-1. Supabaseプロジェクト作成
- [x] [supabase.com](https://supabase.com) でアカウント作成（無料プラン）
- [x] 新規プロジェクトを作成
- [x] 以下の情報を控える:
  - `SUPABASE_URL` (プロジェクトURL)
  - `SUPABASE_SERVICE_KEY` (サービスロールキー)
### 1-2. pgvector拡張とテーブル作成
- [x] Supabase ダッシュボード → SQL Editor で以下を実行:

```sql
-- pgvector拡張を有効化
create extension if not exists vector;

-- ドキュメント格納テーブル
create table documents (
  id bigserial primary key,
  content text,
  metadata jsonb,
  embedding vector(1536)
);

-- 類似度検索用の関数
create or replace function match_documents (
  query_embedding vector(1536),
  match_count int default 3,
  filter jsonb default '{}'
)
returns table (
  id bigint,
  content text,
  metadata jsonb,
  similarity float
)
language plpgsql
as $$
begin
  return query
  select
    documents.id,
    documents.content,
    documents.metadata,
    1 - (documents.embedding <=> query_embedding) as similarity
  from documents
  where documents.metadata @> filter
  order by documents.embedding <=> query_embedding
  limit match_count;
end;
$$;
```

---

## フェーズ2: ローカル環境の準備

### 2-1. 依存パッケージの更新
- [x] `requirements.txt` を更新:

```txt
streamlit==1.32.0

langchain==0.1.16
langchain-openai==0.1.3
langchain-community==0.0.24

openai==1.35.13
supabase==2.4.0
vecs==0.4.3
tiktoken==0.7.0
numexpr==2.10.1
pypdf==4.2.0

python-dotenv==1.0.1
pandas==2.2.2
numpy==1.26.4
```

- [x] `pip install -r requirements.txt` で依存関係をインストール

### 2-2. 環境変数の追加
- [x] `.env` に Supabase の認証情報を追加:

```
OPENAI_API_KEY=sk-xxxxx
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_SERVICE_KEY=eyJxxxxx
```

- [x] `.gitignore` に `.env` が含まれていることを確認

---

## フェーズ3: コード改修

### 3-1. `config.py` の更新
- [x] Supabase 接続情報を追加:

```python
# Supabase設定
SUPABASE_URL = os.getenv("SUPABASE_URL")
SUPABASE_SERVICE_KEY = os.getenv("SUPABASE_SERVICE_KEY")
```

- [x] FAISS関連の設定（`VECTOR_STORE_PATH`）を削除

### 3-2. `modules/rag/vector_store.py` の改修
- [x] FAISS → Supabase (SupabaseVectorStore) に置き換え
- [x] 主な変更点:
  - `FAISS.from_documents()` → `SupabaseVectorStore.from_documents()`
  - `similarity_search()` → Supabaseのmatch_documents関数経由で検索
  - ローカルファイル保存/読み込み (`save_local`, `load_local`) → 不要になるため削除
- [x] pickle による文書メタデータ保存も不要になるため削除

### 3-3. `modules/rag/retriever.py` の改修
- [x] ベクトルストアの初期化方法を Supabase 対応に変更
- [x] `load_existing_documents()` のローカルファイル読み込み処理を見直し
  - Supabase に既にデータがあれば自動的に検索可能なため、起動時の読み込みは不要

### 3-4. `chatbot.py` の改修
- [x] `get_system_status()` のドキュメント数取得を Supabase クエリに変更

### 3-5. `app.py` の改修（必要に応じて）
- [x] 基本的に変更不要（UIレイヤーはそのまま）
- [x] 文書クリア機能を Supabase のテーブル削除に対応させる

---

## フェーズ4: ローカルテスト

### 4-1. 基本動作テスト
- [x] `streamlit run app.py` でローカル起動
- [x]] PDFファイルをアップロードし、正常に登録されることを確認
- [x] アップロードしたPDFに関する質問をし、正しく回答されることを確認
- [x] 参考文書（ファイル名・ページ番号）が表示されることを確認

### 4-2. エッジケーステスト
- [ ] 文書未登録時の質問 → 適切なメッセージが返ることを確認
- [ ] Agentsモード（計算質問）が引き続き動作することを確認
- [ ] 文書クリア → Supabase 上のデータが削除されることを確認
- [ ] 複数PDFアップロード → すべて検索対象になることを確認

---

## フェーズ5: Streamlit Community Cloud へデプロイ

### 5-1. GitHubリポジトリの準備
- [ ] コードを GitHub にプッシュ
- [ ] `.env` がコミットされていないことを最終確認
- [ ] `requirements.txt` が最新であることを確認

### 5-2. Streamlit Community Cloud 設定
- [ ] [share.streamlit.io](https://share.streamlit.io) にログイン
- [ ] 「New app」からリポジトリ・ブランチ・`app.py` を指定
- [ ] 「Advanced settings」→「Secrets」に環境変数を設定:

```toml
OPENAI_API_KEY = "sk-xxxxx"
SUPABASE_URL = "https://xxxxx.supabase.co"
SUPABASE_SERVICE_KEY = "eyJxxxxx"
```

### 5-3. デプロイ確認
- [ ] デプロイ完了後、公開URLにアクセス
- [ ] PDFアップロード → 質問応答の一連の流れをテスト
- [ ] アプリ再起動後もデータが保持されていることを確認（Supabase移行の最大のメリット）

---

## フェーズ6: 要件定義書の更新

- [ ] `要件定義書.md` のベクトルDB記載を「FAISS」→「Supabase (pgvector)」に更新
- [ ] 環境変数の記載に `SUPABASE_URL`, `SUPABASE_SERVICE_KEY` を追加

---

## 改修対象ファイル一覧

| ファイル | 変更内容 |
|---|---|
| `config.py` | Supabase接続情報追加、FAISS設定削除 |
| `modules/rag/vector_store.py` | FAISS → Supabase に全面置き換え |
| `modules/rag/retriever.py` | ベクトルストア初期化の変更 |
| `chatbot.py` | ドキュメント数取得の修正 |
| `requirements.txt` | faiss-cpu 削除、supabase 追加 |
| `.env` | Supabase認証情報追加 |
| `要件定義書.md` | ベクトルDB記載更新 |

---

## 注意事項
- Supabase 無料プランの制限: 500MB ストレージ、50,000行
- OpenAI Embeddings (text-embedding-ada-002) のベクトル次元は **1536** → テーブル定義と一致させること
- Streamlit Community Cloud の Secrets は TOML 形式で記述すること